name: CI

on: [push]

jobs:
  opentofu_run:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    outputs:
      main_ip: ${{ steps.get_ips.outputs.main_ip }}
      worker_ip: ${{ steps.get_ips.outputs.worker_ip }}
    steps:
    - uses: actions/checkout@v4
      with:
        ref: ${{ github.ref }}
    
    
    - uses: opentofu/setup-opentofu@v2
    - name: Checking
      run: ls -a
    - name: Tofu initiation
      env:
        TWC_TOKEN: ${{ secrets.TWC_TOKEN }}
        AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
        AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        S3_BUCKET_NAME: ${{ secrets.S3_BUCKET_NAME }}
        OPENTOFU_WORKING_DIR: ${{vars.OPENTOFU_WORKUNG_DIR}}
      working-directory: ./
      run: |
        tofu version
        export TF_VAR_image_id='ami-abc123'
        export TOFU_LOG=DEBUG
        export TF_LOG=TRACE
        tofu -chdir="$OPENTOFU_WORKING_DIR" init -upgrade
        tofu -chdir="$OPENTOFU_WORKING_DIR" plan -no-color
        tofu -chdir="$OPENTOFU_WORKING_DIR" apply -auto-approve -lock-timeout=360s
        tofu -chdir="$OPENTOFU_WORKING_DIR" output
        tofu -chdir="$OPENTOFU_WORKING_DIR" state list | grep main-server
    - name: Tofu IP export
      env:
        OPENTOFU_WORKING_DIR: ${{vars.OPENTOFU_WORKUNG_DIR}}
      run: |
        export MAIN_IP=$(tofu -chdir="${{ vars.OPENTOFU_WORKUNG_DIR }}" output -json main_server_ip | jq -r '.')
        echo $(tofu -chdir="${{ vars.OPENTOFU_WORKUNG_DIR }}" output -json main_server_ip | jq -r '.')
        export WORKER_IP=$(tofu -chdir="${{ vars.OPENTOFU_WORKUNG_DIR }}" output -json argocd_node_ip | jq -r '.')
        echo $(tofu -chdir="${{ vars.OPENTOFU_WORKUNG_DIR }}" output -json argocd_node_ip | jq -r '.')
    - name: Tofu PW export
      env:
        OPENTOFU_WORKING_DIR: ${{vars.OPENTOFU_WORKUNG_DIR}}
      run: |
        export MAIN_PW=$(tofu -chdir="${{ vars.OPENTOFU_WORKUNG_DIR }}" output -json main_server_root_pw | jq -r '.')
        echo $(tofu -chdir="${{ vars.OPENTOFU_WORKUNG_DIR }}" output -json main_server_root_pw | jq -r '.')
        export WORKER_PW=$(tofu -chdir="${{ vars.OPENTOFU_WORKUNG_DIR }}" output -json argocd_node_server_root_pw | jq -r '.')
        echo $(tofu -chdir="${{ vars.OPENTOFU_WORKUNG_DIR }}" output -json argocd_node_server_root_pw | jq -r '.')
    - name: Echo GH outputs
      env:
        OPENTOFU_WORKING_DIR: ${{vars.OPENTOFU_WORKUNG_DIR}}
      run: |
        echo "main_ip=$MAIN_IP" >> $GITHUB_OUTPUT
        echo "worker_ip=$WORKER_IP" >> $GITHUB_OUTPUT
        echo "main_server_root_pw=$MAIN_PW" >> $GITHUB_OUTPUT
        echo "argocd_node_server_root_pw=$WORKER_PW" >> $GITHUB_OUTPUT

  ansible_setup:
    runs-on: ubuntu-latest
    needs: opentofu_run
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      - name: Install Ansible and dependencies
        run: |
          sudo apt update
          sudo apt install -y ansible sshpass
          ansible-galaxy collection install kubernetes.core
      - name: Add server keys to known_hosts
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan ${{ needs.opentofu_run.outputs.main_ip }} >> ~/.ssh/known_hosts
          ssh-keyscan ${{ needs.opentofu_run.outputs.worker_ip }} >> ~/.ssh/known_hosts

      - name: Create Ansible inventory
        run: |
          cat > inventory.ini << EOF
          [master]
          ${{ needs.opentofu_run.outputs.main_ip }} ansible_user=root ansible_password='${{ needs.opentofu_run.outputs.main_server_root_pw }}'

          [worker]
          ${{ needs.opentofu_run.outputs.worker_ip }} ansible_user=root ansible_password='${{ needs.opentofu_run.outputs.argocd_node_server_root_pw }}'

          [all:vars]
          ansible_ssh_common_args='-o StrictHostKeyChecking=no'
          EOF

      - name: Run Ansible playbook
        run: ansible-playbook -i inventory.ini ansible/playbooks/k3s_with_argocd.yml

      # - name: Write SSH private keys
      #   run: |
      #     mkdir -p ~/.ssh
      #     echo "${{ secrets.SSH_PRIVATE_KEY_FOR_MAIN_SERVER }}" > ~/.ssh/main-server.key
      #     echo "${{ secrets.SSH_PRIVATE_KEY_FOR_ARGOCD_NODE_SERVER }}" > ~/.ssh/argocd-node.key
      #     chmod 600 ~/.ssh/*.key

      # - name: Create Ansible inventory
      #   run: |
      #     cat > inventory.ini <<EOF
      #     [all:vars]
      #     ansible_user=root
      #     ansible_ssh_common_args='-o StrictHostKeyChecking=no'
      #     [main]
      #     main-server ansible_host=${{ needs.opentofu_run.outputs.main_ip }} ansible_ssh_private_key_file=~/.ssh/main-server.key
      #     [worker]
      #     argocd-node ansible_host=${{ needs.opentofu_run.outputs.worker_ip }} ansible_ssh_private_key_file=~/.ssh/argocd-node.key
      #     EOF
      # - name: Checking
      #   run: |
      #     echo ${{ needs.opentofu_run.outputs.main_ip }}
      #     echo ${{ needs.opentofu_run.outputs.worker_ip }}

      # - name: Run Ansible playbook to install k3s and ArgoCD
      #   run: |
      #     ansible-playbook -i inventory.ini ansible/playbooks/k3s_with_argocd.yml
  # ansible_run:
  #   needs: opentofu_run
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v3
  #     - name: Check
  #       run: |
  #         echo ${{ needs.opentofu_run.outputs.main_server_ip }}
  #         echo $MAIN_IP
  #         echo ${{ needs.opentofu_run.outputs.argocd_node_ip }}
  #         echo $WORKER_IP
  #     - name: Getting IPs from OpenTofu
  #       id: get_ips
  #       run: |
  #         echo "main_ip=${{ needs.opentofu_run.outputs.main_server_ip }}" >> $GITHUB_OUTPUT
  #         echo "worker_ip=${{ needs.opentofu_run.outputs.argocd_node_ip }}" >> $GITHUB_OUTPUT
  #     - name: Install Ansible & Co.
  #       run: |
  #         sudo apt update
  #         sudo apt install -y ansible python3-pip sshpass
  #         pip3 install --upgrade ansible
  #     - name: Setup SSH
  #       env:
  #         SSH_PRIVATE_KEY_FOR_ARGOCD_NODE_SERVER: ${{ secrets.SSH_PRIVATE_KEY_FOR_ARGOCD_NODE_SERVER }}
  #         SSH_PRIVATE_KEY_FOR_MAIN_SERVER: ${{ secrets.SSH_PRIVATE_KEY_FOR_MAIN_SERVER }}
  #       run: |
  #         mkdir -p ~/.ssh
  #         echo "$SSH_PRIVATE_KEY_FOR_MAIN_SERVER" > ~/.ssh/main-server-ssh-key
  #         chmod 600 ~/.ssh/main-server-ssh-key
  #         echo "$SSH_PRIVATE_KEY_FOR_ARGOCD_NODE_SERVER" > ~/.ssh/argocd-node-server
  #         chmod 600 ~/.ssh/argocd-node-server
  #         echo "Host *" > ~/.ssh/config
  #         echo "  StrictHostKeyChecking no" >> ~/.ssh/config
  #         echo "  UserKnownHostsFile=/dev/null" >> ~/.ssh/config
  #         echo "  User root" >> ~/.ssh/config
  #     - name: Init inventory
  #       env:
  #         MAIN_IP: ${{ needs.opentofu_run.outputs.main_server_ip }}
  #         WORKER_IP: ${{ needs.opentofu_run.outputs.argocd_node_ip }}
  #       run: |
  #         python3 scripts/generate_inventory.py \
  #           --master "$MAIN_IP" \
  #           --worker "$WORKER_IP" \
  #           --output inventory.ini
  #         cat inventory.ini 